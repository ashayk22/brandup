import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Hides the floating Next.js dev tools button in the bottom-left corner
  // during `next dev`. Purely a dev-mode overlay — has no effect on
  // production builds either way.
  devIndicators: false,
};

export default nextConfig;
