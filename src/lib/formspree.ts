/** Formspree form that receives both the contact section and the
 *  "Start a project" dialog. Override with NEXT_PUBLIC_FORMSPREE_ID if you
 *  ever point the site at a different form. */
export const FORMSPREE_FORM_ID =
  process.env.NEXT_PUBLIC_FORMSPREE_ID ?? "mkjgoggy";
